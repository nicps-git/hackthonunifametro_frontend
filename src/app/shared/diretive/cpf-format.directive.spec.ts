import { CpfFormatDirective } from './cpf-format.directive';

describe('CpfFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CpfFormatDirective();
    expect(directive).toBeTruthy();
  });
});
